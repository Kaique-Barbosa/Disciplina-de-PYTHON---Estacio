#print(list(range(10, 0 , 1)))

#var_lista = [] #var_lista = List()
#print(var_lista)

# metodos de Lista para adicionar indices
#var_lista.append("Estacio")
#print(var_lista)
#print(var_lista[1])

# mudou / atualizou o valor do indice 1
#var_lista[1] = "unversidade"
#print(var_lista)

# metodos de Lista para remover valores dos indices
#del var_lista[0]
#var_lista.pop(2)

lista_produtos = []

for i in range(2):
    lista_produtos.append(input("Digite o produto:").upper())


for produtos in lista_produtos:
    print(f"Produto: {produtos}")


total = 0
preco = 1

while preco > 0:
    preco = float(input("Digite o preço: \n"))
    
    total += preco
    
print(f"Total a pagar R${total}")

